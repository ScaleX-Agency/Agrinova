// types/dashboard.ts
// Shared types for the dashboard — import from here rather than re-declaring.

export type DateRange = "this-month" | "last-month" | "this-quarter" | "this-year";

export type DashboardTab = "sales" | "inventory" | "finance" | "operations";
